<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:name=".ZenithApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/tv_banner"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zenith"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".MainActivity" android:exported="true" android:theme="@style/Theme.Zenith.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zenith" android:host="player" />
                <data android:scheme="zenith" android:host="detail" />
                <data android:scheme="zenith" android:host="series" />
            </intent-filter>
            <!-- Раньше это ни на что не было завязано: MovieSearchProvider отдавал
                 выдуманную строку, поэтому системе просто нечего было запускать
                 через ACTION_SEARCH. Теперь голосовой/текстовый ввод в системном
                 поиске TV, не только выбор конкретной подсказки, тоже долетает сюда. -->
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable" android:resource="@xml/searchable" />
        </activity>
        <meta-data android:name="android.app.default_searchable" android:value=".MainActivity" />
        <!-- readPermission ограничивает, кто может дёргать этот провайдер:
             раньше exported=true без readPermission означало, что ЛЮБОЕ
             приложение на устройстве могло запрашивать подсказки (сейчас —
             реальные названия из вашего каталога) напрямую, в обход системного
             поиска. GLOBAL_SEARCH — permission именно фреймворка поиска. -->
        <provider android:name=".provider.MovieSearchProvider" android:authorities="com.platinum.ott.search" android:exported="true" android:readPermission="android.permission.GLOBAL_SEARCH" />
    </application>
</manifest>
